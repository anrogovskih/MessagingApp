<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.axmor.fsinphone.videomessages.ui.screens.edit_contact.EditContactViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/toolbar"
            layout="@layout/view_toolbar_with_button"
            app:buttonBackClickObservable="@{viewModel.stateLiveData.buttonBackClickObservable}"
            app:contentDescription="@{@string/common_save}"
            app:customButtonClick="@{viewModel.stateLiveData.buttonSaveClickObservable}"
            app:customButtonIcon="@{@drawable/ic_done}"
            app:title="@{@string/edit_contact_edit_profile}" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/toolbar">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <com.axmor.fsinphone.videomessages.ui.customViews.AvatarView
                    android:id="@+id/avatar"
                    android:layout_width="@dimen/contact_details_avatar_size"
                    android:layout_height="@dimen/contact_details_avatar_size"
                    android:layout_marginTop="48dp"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:urlOrBase64="@{viewModel.stateLiveData.editedAvatar}" />

                <ImageButton
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_clear"
                    android:background="?selectableItemBackgroundBorderless"
                    app:isInvisible="@{viewModel.stateLiveData.isDefaultAvatar}"
                    app:toggle="@{viewModel.stateLiveData.buttonClearAvatarClickObservable}"
                    app:layout_constraintCircleAngle="45"
                    app:layout_constraintCircle="@+id/avatar"
                    app:layout_constraintCircleRadius="56dp"
                    tools:ignore="MissingConstraints" />

                <TextView
                    android:id="@+id/tv_edit_photo"
                    style="@style/TextButton"
                    android:layout_width="wrap_content"
                    android:layout_marginTop="24dp"
                    android:paddingHorizontal="24dp"
                    android:text="@string/edit_contact_edit_photo"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/avatar"
                    app:toggle="@{viewModel.stateLiveData.buttonEditAvatarClickObservable}" />

                <com.google.android.material.textfield.TextInputLayout
                    style="@style/InputField"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginHorizontal="32dp"
                    android:layout_marginTop="48dp"
                    android:maxWidth="320dp"
                    android:paddingBottom="16dp"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/tv_edit_photo"
                    app:layout_constraintVertical_bias="0">

                    <com.google.android.material.textfield.TextInputEditText
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:lines="1"
                        android:maxLength="70"
                        android:maxLines="1"
                        android:singleLine="true"
                        android:text="@={viewModel.stateLiveData.editedName}" />
                </com.google.android.material.textfield.TextInputLayout>

                <ProgressBar
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="@{viewModel.loadingLiveData? View.VISIBLE : View.INVISIBLE}"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />
            </androidx.constraintlayout.widget.ConstraintLayout>
        </ScrollView>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>