<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.axmor.fsinphone.videomessages.ui.screens.main.MainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/toolbar"
            layout="@layout/view_toolbar_with_logo_and_settings"
            app:settingsClick="@{viewModel.stateLiveData.settingsClick}"/>

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefreshLayout"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:visibility="@{viewModel.stateLiveData.isErrorScreenShown? View.INVISIBLE : View.VISIBLE}"
            tools:visibility="visible"
            app:layout_constraintTop_toBottomOf="@+id/toolbar"
            app:layout_constraintBottom_toBottomOf="parent"
            app:onRefresh="@{viewModel.stateLiveData.isRefreshing()}"
            app:refreshing="@{viewModel.stateLiveData.isRefreshing()}">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:orientation="vertical"
                android:clipToPadding="false"
                android:paddingTop="@dimen/main_top_space"
                android:paddingBottom="@dimen/main_bottom_space"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:itemCount="1"
                tools:listitem="@layout/view_chat_contact"
                />

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <com.google.android.material.button.MaterialButton
            style="@style/Button"
            android:layout_width="0dp"
            android:layout_marginHorizontal="56dp"
            android:layout_marginBottom="24dp"
            android:text="@string/common_refill_balance"
            android:onClick="@{() -> viewModel.onBalanceRefillClicked()}"
            android:visibility="@{viewModel.stateLiveData.isErrorScreenShown || viewModel.prisoners.size == 0? View.INVISIBLE : View.VISIBLE}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintWidth_max="300dp"/>

        <include layout="@layout/view_error_screen"
            bind:isVisible="@{viewModel.stateLiveData.isErrorScreenShown}"
            bind:clickObservable="@{viewModel.stateLiveData.updateClickObservable}"
            bind:isNetworkError="@{viewModel.stateLiveData.isNetworkError}"/>

        <include layout="@layout/view_progress_bar_for_constraint_layout_with_toolbar"
            bind:isLoading="@{viewModel.loadingLiveData}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
